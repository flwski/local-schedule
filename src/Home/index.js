import React from 'react';
import { View, ScrollView } from 'react-native';

// import { Container } from './styles';

const Home = () => {
    return (<>
        <ScrollView>
            <View>

            </View>
        </ScrollView>

    </>);
}

export default Home;